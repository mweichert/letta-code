upstream:
  remote: upstream
  branch: main

base: main

branches:
  - name: feature/fork-management
    base: main
    description: Fork management infrastructure (build script, config, docs)
    docs: branches/feature/fork-management.md
